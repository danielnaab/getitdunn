version: "3.9"

services:
  middleman:
    build: .
    volumes:
      - .:/src
      - ~/.ssh:/root/.ssh
    working_dir: /src
    ports:
      - "4567:4567"
    command: bundle exec middleman server
